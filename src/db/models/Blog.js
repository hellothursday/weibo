/**
 * @description 用户数据模型
 * @author qe
 */

const seq = require('../seq')
// eslint-disable-next-line
const {STRING, TINYINT} = require('../types')

// blogs 表
const Blog = seq.define('user', {
    // TODO Blog
})

module.exports = Blog
